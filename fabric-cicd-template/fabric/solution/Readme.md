Place the Fabric item folders synchronized from the DEV workspace in this directory.

Example:

```text
lh_solution.Lakehouse/
nb_transform.Notebook/
pl_orchestrator.DataPipeline/
```

Every supported item folder found here is deployed automatically.
